name: Deploy My Static Site to Walrus with Provenance

on:
  release:
    types: [published]

permissions:
  id-token: write
  contents: write
  actions: read

jobs:
  build:
    uses: zkzktx-io/sui-mvr-provenance@v0.0.3
    with:
      working-directory: ./hello_world
